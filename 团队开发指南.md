# 开发指南

本文档详细说明了 Campus Assistant 项目的分支策略、开发流程，所有团队成员在开发过程中应严格遵守。

---

## 📌 目录

- [分支策略](#分支策略)
- [开发流程](#开发流程)
---

## 🌿 分支策略

本项目采用 **Git Flow** 分支管理策略，确保代码质量和版本控制的规范性。

### 分支类型

| 分支类型 | 命名规则 | 说明 | 生命周期 | 来源 | 目标 |
|---------|---------|------|---------|------|------|
| **main** | `main` | 生产环境稳定版本 | 永久 | - | - |
| **dev** | `dev` | 开发集成分支 | 永久 | main | main |
| **feature** | `feature/*` | 新功能开发 | 临时 | dev | dev |
| **fix** | `fix/*` | Bug 修复 | 临时 | dev | dev |
# 分支详细说明

#### 1. main 分支（主分支）

- **用途**：存放生产环境的稳定代码
- **保护规则**：
    - ❌ 禁止直接推送代码
    - ✅ 只能通过 Pull Request 从 dev 或 hotfix 合并
    - ✅ 每次合并必须通过 CI 检查
    - ✅ 需要至少 1 人 Code Review

~~~
查看 main 分支
git checkout main 
git pull origin main
~~~


#### 2. dev 分支（开发分支）

- **用途**：日常开发集成，包含最新的功能
- **保护规则**：
  - ❌ 禁止直接推送（建议）
  - ✅ 从 feature/fix 分支合并
  - ✅ 需要通过 CI 检查

~~~
切换到 dev 分支
git checkout dev 
git pull origin dev
~~~
#### 3. feature 分支（功能分支）

- **用途**：开发新功能
- **命名规范**：`feature/功能描述`
- **示例**：
  - `feature/user-authentication`
  - `feature/course-management`
  - `feature/file-upload`

~~~
创建功能分支
git checkout dev 
git pull origin dev 
git checkout -b feature/user-authentication（以具体开发功能命名的分支）
开发完成后合并回 dev
git checkout dev 
git merge feature/user-authentication 
git push origin dev
删除已合并的功能分支
git branch -d feature/user-authentication 
git push origin --delete feature/user-authentication
~~~


#### 4. fix 分支（修复分支）

- **用途**：修复非紧急 Bug
- **命名规范**：`fix/问题描述`
- **示例**：
  - `fix/login-validation`
  - `fix/database-connection`


## 开发流程

### 标准流程

~~~
创建分支 → 本地开发 → 提交代码 → 推送远程 
→ 创建 PR → Code Review → CI 检查 → 合并 → 删除分支
~~~


### 快速开始

~~~
1. 创建功能分支
git checkout dev
git pull origin dev
git checkout -b feature/功能名称

2. 开发并提交
git add . git commit -m "feat: 功能描述"

3. 推送到远程
git push origin feature/功能名称

4. 创建 Pull Request（GitHub/Gitee 网页操作）

5. 合并后清理
git checkout dev 
git merge feature/功能名称 
git branch -d feature/功能名称 
git push origin --delete feature/功能名称
~~~


### 日常习惯

- **开始前**：拉取最新代码 `git pull origin dev`
- **工作中**：频繁小步提交，保持原子性
- **完成后**：及时创建 PR，避免长期分支
- **合并后**：立即删除已合并分支

### 注意事项

⚠️ **禁止**直接推送到 `main` 和 `dev` 分支  
⚠️ **必须**通过 Pull Request 合并代码  
⚠️ **确保** CI 检查通过后再合并  
⚠️ **及时**处理 Review 意见
